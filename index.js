import { colors, gradients, shadows, spacing, borderRadius, typography } from './colors';

export const theme = {
  colors,
  gradients,
  shadows,
  spacing,
  borderRadius,
  typography,
};

export { colors, gradients, shadows, spacing, borderRadius, typography };
export default theme;
